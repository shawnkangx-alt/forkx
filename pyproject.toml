[project]
name = "forkx"
version = "0.1.0"
description = "FORKX — 个人炒股助理"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
forlsx = "forkx.code.main:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
